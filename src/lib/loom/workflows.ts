/**
 * Loom-facing view of the existing WORKFLOWS registry.
 * Thin re-export — the workflow content lives in src/lib/modes/workflows.ts
 * and is not duplicated here.
 */
export { WORKFLOWS, type WorkflowTemplate, type WorkflowStep } from "@/lib/modes/workflows";